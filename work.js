const form = document.getElementById('form')
const firstname_input = document.getElementById('uname')
const password_input = document.getElementById('pword')
const repeat_password_input = document.getElementById('rpword')
form.addEventListener('submit', (e) => {

})